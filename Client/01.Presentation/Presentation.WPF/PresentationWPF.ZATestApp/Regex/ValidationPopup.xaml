<Popup x:Class="PresentationWPF.ZATestApp.Regex.ValidationPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" PopupAnimation="Slide" StaysOpen="False" IsOpen="True"            
             MouseDown="Popup_MouseDown">

    <Border BorderBrush="Black" Background="{DynamicResource {x:Static SystemColors.InfoBrushKey}}" 
            BorderThickness="1">
        <StackPanel Orientation="Vertical">
            <StackPanel.Resources>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="Margin" Value="4"/>
                </Style>
            </StackPanel.Resources>
            <TextBlock x:Name="TitleTextBlock" FontWeight="Bold"/>
            <TextBlock x:Name="MessageTextBlock" />
        </StackPanel>
    </Border>

    </Popup>
